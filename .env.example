# Demo mode is self-contained and requires no credentials.
NEXT_PUBLIC_APP_MODE=demo

# Production connectors are intentionally not implemented in this repository.
# Configure secrets only through the target platform's secret manager.
# ANAF_CLIENT_ID=
# ANAF_CLIENT_SECRET=
# ERP_API_BASE_URL=
# ERP_API_TOKEN=
